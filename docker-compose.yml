services:
  preppr:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/data/dev.db
      - UPLOAD_DIR=/data/uploads
      - APP_LOCALE=de
    restart: unless-stopped
